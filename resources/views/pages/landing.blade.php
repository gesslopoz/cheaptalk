@extends('base')

@section('content')
<div class="container">
    <div class="card mt-4">
        {{-- style="; --}}
    <div class=" items-center justify-center text-center">
        <div class="card-body bg-dark">
            <h1 class="text-white">Welcome to Midterm CheapTalk!</h1>
            <!-- @if (!Auth::check())
            <a href="{{url('/register')}}"
                class="btn btn-primary">
            Get Started
            </a>
            @endif -->

        </div>
    </div>
</div>
</div>
@endsection